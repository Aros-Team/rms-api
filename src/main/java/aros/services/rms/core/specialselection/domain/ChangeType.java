package aros.services.rms.core.specialselection.domain;

/** Enumerates the types of changes that can be recorded in the special selection history. */
public enum ChangeType {
  CREATE,
  UPDATE,
  DELETE,
  SCHEDULE_CHANGE,
  PRICE_CHANGE
}
